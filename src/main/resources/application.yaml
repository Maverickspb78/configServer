server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Maverickspb78/CloudConfigSpring
          search-paths: shopConfig
          clone-on-start: true
  application:
    name: storeConfidDB

logging:
  level:
    org.springframework.cloud: DEBUG
